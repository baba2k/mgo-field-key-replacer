# mgo-field-key-replacer
Replaces all MongoDB field keys in every document from a collection by a replace map.

## General
* Replaces field keys by a map[string]string{"oldKey":"neyKey"}

## Examples
[Simple example](./example/main.go)